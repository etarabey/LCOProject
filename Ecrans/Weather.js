import * as React from 'react'; 
import {view,text,Pressable} from 'react-native';
import { Accueil } from './Accueil';
import {Text,View,SafeAreaView} from 'react-native';
export function Weather(){ 
    return( 
        <SafeAreaView>
        <Text>Bonjour Paris</Text>
        </SafeAreaView>
    );
}
